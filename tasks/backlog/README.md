# Backlog planificado

Estas tareas tienen identificadores y límites estables, pero deben satisfacer dependencias y reemplazar cualquier validación marcada `GATE` por el comando exacto acordado antes de moverse a `ready/`.
