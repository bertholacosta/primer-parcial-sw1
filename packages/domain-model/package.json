{
  "name": "domain-model",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Representación versionada del modelo canónico (contrato domain-model v1) y utilidades puras.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
