{
  "name": "model-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Servidor de modelo: repositorio en memoria del modelo canónico y hospedaje de sesiones de colaboración (collaboration-protocol v1; ADR-0003, Alternativa A).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "collaboration-protocol": "file:../../packages/collaboration-protocol"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
