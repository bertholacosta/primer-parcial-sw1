{
  "name": "generator-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Núcleo del generador determinista Spring Boot: lectura, validación, planificación ordenada y escritura atómica (contrato generator-input-output v1, ADR-0002).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "generator-cli": "./dist/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deps:build": "npm --prefix ../../packages/domain-model install && npm --prefix ../../packages/domain-model run build && npm --prefix ../../packages/domain-validator install && npm --prefix ../../packages/domain-validator run build",
    "prebuild": "npm run deps:build",
    "build": "tsc -p tsconfig.json",
    "bundle": "ncc build dist/index.js -o dist -m",
    "lint": "eslint src/**/*.ts",
    "pretest": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "domain-model": "file:../../packages/domain-model",
    "domain-validator": "file:../../packages/domain-validator",
    "handlebars": "^4.7.9"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.16.0",
    "@typescript-eslint/eslint-plugin": "^8.70.0",
    "@typescript-eslint/parser": "^8.70.0",
    "@vercel/ncc": "^0.45.0",
    "eslint": "^10.11.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^3.2.4"
  }
}
